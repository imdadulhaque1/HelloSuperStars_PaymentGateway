import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const LineView = () => {
  return (
    <View style={{borderWidth:0.2,borderColor:'gray',margin:10}} />
  )
}

export default LineView

const styles = StyleSheet.create({})